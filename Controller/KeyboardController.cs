using UnityEngine;
using System.Collections;

/// <summary>
/// This needs observer pattern or something so that it keeps up to date with the selected character list,
/// which can be changed from mouseController
/// </summary>
public class KeyboardController : UserInputController
{
    // Update is called once per frame
    void Update()
    {
        //if (Input.GetButton("9"))
        //{
        //    selectedCharacter.equipedWeapon = new Dagger();
        //}
    }
}
