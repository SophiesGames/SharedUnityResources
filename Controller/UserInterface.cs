using UnityEngine;
using System.Collections;


/// <summary>
/// This takes comands from somewhere but does not listen or know about anything else. It may have to return details btu probably 
/// shouldnt. It should jsut display info based of what it reads and gets told rather than storing any info itself. There
/// may be exceptions.
/// </summary>
public class UserInterface : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
